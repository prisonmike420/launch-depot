# Launch Depot plugin

A skills-only plugin. It does not bundle an MCP server and does not require its own service credentials.

The skill uses whatever browsing/computer capabilities are already available in the host. If the host cannot browse or interact with websites, the workflow must stop at research/preparation and must not claim that submissions were completed.

See `skills/launch-depot/SKILL.md` for workflow and safety rules.
